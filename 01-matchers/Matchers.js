export function addToNumbers(a ,b) {
  return a + b
}

export function sortIntegerPredicate(a, b) {
  if (a < b) return -1
  if (a > b) return 1
  return 0
}

export const shoppingList = [
  'milk',
  'cookies',
  'bread'
]

